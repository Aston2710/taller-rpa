# Carpeta con los archivos de entrada, organizados por fecha (year/month/day/*.csv|*.xlsx)
INPUT_PATH=data/input
# Carpeta donde se espejan los resultados (misma ruta/nombre que la entrada) y logs/
OUTPUT_PATH=data/output
# Formulario web destino
WEB_FORM_URL=https://example.com/form
HEADLESS=true
